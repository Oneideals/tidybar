# 授权后的原生折叠验收 · 2026-09-09

用户确认授权后，沿用 `b82694b0526cb55e66a5b376481179931109e5a40d11adf14c54b45fb3b8ad99` 测试包，未重编译应用。重启后的 PID 为 `25636`，启动日志明确报告“辅助功能权限 = 已授予｜模式 = 完整接管”，随后完成物理分组。此前的权限阻塞已经消除。

## 当前实际结果

| 项目 | 观察与结论 |
| --- | --- |
| 现有隐藏分组 | 设置窗口显示 FSMenuApp、OrbStack 共 2 项；显示区 28 项、始终隐藏区 0 项。 |
| 折叠 | FSMenuApp 的 x 为 `-1762`，OrbStack 的 x 为 `-1717`，均已离开主屏可见范围；入口显示 `▶`。 |
| 展开 | 点击实际 TidyBar 入口后，FSMenuApp 回到 `x=388`、OrbStack 回到 `x=358`，入口变为 `◀`；约 `0.21s` 后读到展开位置。 |
| 自动收起 | 无后续输入时，约 `2.13s` 后分隔符再次回到屏外，符合默认 2 秒设置。 |
| 展开区的可见性限制 | Otty 的应用菜单覆盖了展开区最左侧；左分隔符的实际命中仍为 Otty 的“帮助”菜单。FSMenuApp 在截图中可见，OrbStack 的屏内坐标仍落在应用菜单区域。不能把“回到屏内”直接算作两个图标都可见、可点击。 |
| 新加入隐藏区 / 始终隐藏 | 尚未完成。准备通过设置窗口临时调整 Paste 时，Mac 已自动锁屏；后台点击返回无法验证，未算作成功。 |

本次基本折叠利用了已经位于分区边界内的两项；未据此宣称新图标的物理重排也已通过。

## 证据

- [当前进程日志](../../.build/native-fold-20260909-3/app-authorized.stderr.log)：授权、完整接管与物理分组完成。
- [折叠后的菜单栏](../../.build/native-fold-20260909-3/after-start-menubar.png) / [展开时的菜单栏](../../.build/native-fold-20260909-3/expanded-menubar.png)。
- [折叠后的枚举](../../.build/native-fold-20260909-3/after-start-enumeration.log) / [展开后的枚举](../../.build/native-fold-20260909-3/expanded-enumeration.log)。
- [展开及自动收起采样](../../.build/native-fold-20260909-3/expand-controls.log)：真实入口 AXPress 返回 `pressed`，随后记录分隔符位置变化及命中结果。
- [已授权的设置窗口](../../.build/native-fold-20260909-3/after-start-settings.png)。

原始全屏截图单独保留在同一证据目录，正文引用从原图裁出的顶部 60 像素；未将应用菜单遮挡加工成可见。

## 配置保护

当前磁盘布局为 35 项：隐藏 7、显示 28，其中隐藏记录包含 5 条系统项；系统项不参与自动隐藏，因此界面实际可管理的隐藏项为 2 个。配置 SHA-256 仍为：

```text
25eadebe54311947db7d0df48ec567d1ace7da1b47bfe3b12705f47467a0ff67
```

该文件与本次 `before-authorized-test-support` 备份逐字节一致。尚未修改 Paste 或其它用户图标的分组。更早的 2 / 30 / 3 分组备份不代表用户当前选择。

## 待继续验证

Mac 锁屏后，`CGSessionCopyCurrentDictionary` 确认 `locked=1`，截图工具明确拒绝截图。`mac idle` 的空闲绿灯和 `mac windows` 的 `on=1` 均不足以证明会话已经解锁；后续输入前须直接检查交互会话状态。

解锁后继续验证新加入隐藏区的实际重排、始终隐藏以及展开区被应用菜单遮挡时的访问方式。临时测试分组须恢复；保持同一测试包，避免无必要地改变签名。

本轮无源代码修改，应用的 Debug / Release 回归记录仍为各 279 条、40 个套件通过。完整原生交互验收尚未完成。
