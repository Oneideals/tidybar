# M0 技术验证清单（4 周）

目的：把全项目最大的技术风险前置。菜单栏工具的两个高危环节——**枚举别人的图标**和**替用户拖动图标**——都依赖未公开契约，任何一次系统大版本更新都可能失效。Bartender 5/6 在 macOS Tahoe 上的崩溃（幽灵点击、光标劫持、图标错乱）就是活生生的反例。

M0 的通过标准不是"功能做完了"，而是**知道它在什么条件下会坏、坏了会不会伤人**。

## 进度

- [x] **验证项 1 图标枚举：通过** → 结论与实测数据见 [findings/01-enumeration.md](findings/01-enumeration.md)
- [x] **验证项 2 ⌘ 拖拽与事件哨兵：通过**（100/100，零卡键）→ [findings/02-drag.md](findings/02-drag.md)
- [x] **验证项 3 崩溃与强杀安全：通过**（强杀无输入残留、图标不损坏、孤儿意图重放收敛）→ [findings/03-crash-safety.md](findings/03-crash-safety.md)
  - 附带抓到主路径 bug：引擎里残留已被证伪的光标预检 → 职责重划（光标归 mover，结果复核归引擎）
  - 曾未决的 SIGTERM 缺口已闭合：信号收尾 + 半空拖拽确定性抬起 + 重放两次即放弃，均真机复验（同文档"已补"一节）
- [x] **验证项 4 占用与功耗：通过**（内存 13MB / 空闲 CPU 0.0% / 包 932KB / 4 线程，四项预算全达标）→ [findings/04-performance.md](findings/04-performance.md)
  - 方法论修正：闸门改为从 `LayoutEngine.apply` 打（`--via-engine`），直连 mover 的绿灯不再算数
  - 未决项：Energy 评级与 1 小时静置未量、位图缓存真机未证

**M0 四项验证全部通过，进入 M1。** 真实环境闸门已在 35 图标的真实菜单栏上跑过 48 轮 / 96 次拖拽全绿
（被拖对象是我们自己的 ☰ 图标，邻居全是用户真实图标）→ [findings/05-real-bar-gate.md](findings/05-real-bar-gate.md)。
这一轮暴露并修掉了"结果复核读帧太急"的误判 bug（只在真实菜单栏上出现）。
剩下未做的字面判据：**直接拖动用户自己 App 的图标**（需单独授权指定目标 App）。完成前 `isConfirmedSupportedOS` 保持 false。

## 前置条件

- 本机 macOS 26.x（Tahoe），带刘海的 MacBook 优先；有条件再借一台非刘海机型。
- 授予辅助功能权限：系统设置 → 隐私与安全性 → 辅助功能。
- 先跑 `swift run tidybar-checks` 确认基线全绿（当前 128 条 / 22 套件）。

## 验证项 1：图标枚举可行性（第 1 周）

目标：把 `PlaceholderMenuBarReader` 换成真实现，并确认覆盖率。

1. 在 `Sources/TidyBarCore/Accessibility/` 新增 `AccessibilityMenuBarReader`，用 `AXUIElementCreateSystemWide` + 菜单栏 AX 元素枚举子项，产出 `ManagedItem.Discovery`。
2. 记录并对比以下 App 的图标是否可枚举、title 是否稳定、frame 是否准确：系统时钟/控制中心/电池/Wi-Fi，第三方 Dropbox/微信/Raycast/iStat Menus，以及一个自研 `NSStatusItem`（用 `scripts/fixture-statusitem.sh` 起 6 个测试图标）。
3. ✅ 已完成。实测记录：90 进程中 30 个暴露 extras，接受 32 项（第三方 28 + 系统 5）、拦截 30 项脏数据。
   - 图标 **消失/新增** 时能否在 500ms 内察觉（对应 A7 新图标策略）。
   - 同一 App 改名/切换语言后，`ManagedItem.stableID` 是否仍指向同一图标（这是配置不被抹掉的前提）。
   - 枚举一次的耗时与调用频率上限，用来定节流值。
4. 产出：`docs/findings/01-enumeration.md`，写清哪些 App 读不到、失败模式是什么。

失败退路：若枚举不稳定，隐藏能力退化为"按 owner App 整体收放"，并在 README 明示限制。

## 验证项 2：⌘ 拖拽与事件哨兵（第 2-3 周）✅ 已完成，结论见 findings/02-drag.md

M0 已收尾，各项实测数据统一见 `docs/findings/`。

目标：证明"能拖"，更要证明"拖不动时会立刻停手"。

1. 实现 `SyntheticDragMenuBarMover`：`CGEvent` 序列 = 按下 ⌘ → 移动到图标中心 → 按下左键 → 分步移动到目标 X → 松开 → 松开 ⌘。全程经 `EventSentinel` 预检/复核。
2. 逐项做破坏性测试，每项都要看到**中止 + 回滚 + 无残留 pending 意图**：
   - 拖拽中途用户真的按住鼠标（应判定 `.userInteracting`）。
   - 拖拽中途用触控板挪动光标（应判定 `.cursorDrift`）。
   - 目标 App 在此期间退出（应判定 `.itemVanished`）。
   - 连续 50 次快速拖拽（应被节流，不得堆队列）。
   - 在系统更新/重启后（应自动落到 `panelOnlyFallback`，且不损坏用户菜单栏）。
3. 记录副作用：拖动过程中是否短暂抢走焦点、是否触发 Mission Control/调度中心手势、Spotlight 弹出时是否互相干扰。
4. 每次实测都必须写进 `hasConfirmedDragSupport` 的判定流程：**未在同一台机器、同一系统版本上连续 100 次全绿，不得开放完整接管**。
5. 产出：`docs/findings/02-drag.md`，附最短可复现脚本。

## 验证项 3：崩溃与强杀安全（第 3 周）✅ 已完成，结论见 findings/03-crash-safety.md

1. 在拖拽执行中 `kill -9` 进程，重启后确认：图标位置未损坏，`LayoutJournal` 的 pending 被识别并重放（用例 `orphanedIntentIsReplayedOnLaunch` 已在，但需要真机验证真实系统状态）。
2. 关机/注销时进程被终止，再登录：布局一致。
3. 产出：把真机结论补进 `LayoutJournal` 的注释，作为"为什么需要这个文件"的依据。

## 验证项 4：占用与功耗（第 4 周）✅ 已完成，结论见 findings/04-performance.md

1. `./scripts/build-app.sh && ./scripts/perf-check.sh --minutes 60`：静置 1 小时，对照 `PerformanceBudget`（内存 40MB、空闲 CPU 0.1%）。
2. Instruments Allocations 检查是否有位图缓存增长（`ImageCache` 上限 20MB）。
3. 活动监视器 Energy 面板读数记录为"低"；若为"高"，先查是否退化成轮询。
4. 刘海屏上面板与图标是否被遮挡（`PanelGeometryTests` 已覆盖纯几何，需真机对照）。
5. 产出：`docs/findings/04-performance.md` + 一组基线数字（发布时公开）。

## M0 Go / No-Go

已满足（实测出处见 findings/）：

- [x] 图标枚举在 10 个真实 App 上稳定，且有明确失败清单（实测 28 个 App，01-enumeration.md）
- [x] ⌘ 拖拽可用且安全中止：自造 fixture 图标上直连 mover 100/100、零卡键；5 类破坏性测试均安全中止（02-drag.md）
  - 注：那 100 次未经过引擎主路径。补 `--via-engine` 后主路径实测 30/30（04-performance.md），其余次数并入下方真实图标复跑
- [x] 强杀恢复在真机验证通过：无输入残留、图标不损坏、孤儿意图重放收敛（03-crash-safety.md）
- [x] 占用达预算：phys_footprint 13MB、空闲 CPU 0.0%、包 932KB、4 线程（04-performance.md）
  - 冷启动接管曾击穿预算（真 app 2.80s）→ 已修：逐进程并发 12 + 单进程 500ms 超时，实测 0.52~1.01s；
    为它补了一条回归用例锁默认节奏，并留了 `scripts/scan-cold-bench.sh` 做参数 A/B

开放真实接管前的剩余闸门（`isConfirmedSupportedOS` 在此之前保持 false）：

- [x] 真实菜单栏环境复跑：48 轮 / 96 次拖拽全绿（被拖=我方图标，邻居=用户真实图标），并抓到只在真实布局下暴露的复核误判 bug（05-real-bar-gate.md）
- [ ] 直接拖动**用户某个 App 的图标**再放回——需要单独授权并指定目标 App（M1 的「点击转发」已单独真机复验，见 findings/06）
- [ ] 静置 1 小时无增长 + 活动监视器 Energy 评级"低"（目前只测到 2 分钟，Energy 需 GUI 读数）
- [ ] 位图缓存真机验证：M1-2 已把抓图路径接上并验过四条判据（findings/07），但缓存字节上限仍是 12 条离线用例在守，未做真机 20 图标长时间观察

任一不过，按报告 §4.4 走降级路线：**只做收纳面板（不动系统图标），把产品定位收敛为"下拉面板 + 搜索 + 规则"**，并据此重写 MVP 范围——不要在不可靠的机制上继续堆功能。
